{
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "production": "npm run build && npm run prod-optimizations",
        "prod-optimizations": "php artisan config:cache && php artisan route:cache && php artisan view:cache"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "lodash": "^4.17.19",
        "postcss": "^8.1.14",
        "vite": "^4.0.0"
    }
}
